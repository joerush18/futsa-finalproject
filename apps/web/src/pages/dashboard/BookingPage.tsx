import { Box } from "@mui/material";

const BookingPage = () => {
  return (
    <Box
      sx={{
        paddingY: "20px",
        height: "105%",
        overflowY: "scroll",
        scrollbarWidth: "none",
      }}
    ></Box>
  );
};

export default BookingPage;
