import DHeader from "@/components/dashboard/DHeader";
import { Box } from "@mui/material";

const HomePage = () => {
  return (
    <Box>
      <DHeader />
    </Box>
  );
};

export default HomePage;
